{
	"name": "camelai-docs-content",
	"private": true,
	"scripts": {
		"dev": "echo 'Use open-mdx-docs: DOCS_DIR=$PWD BASE_PATH=/docs bun run dev' && exit 1",
		"deploy": "echo 'Deploy from https://github.com/Vercantez/open-mdx-docs (see DEPLOY.md)' && exit 1",
		"ci:deploy": "echo 'Deploy from open-mdx-docs, not this repo' && exit 1"
	}
}
