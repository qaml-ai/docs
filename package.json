{
  "scripts": {
    "dev": "mint dev",
    "deploy": "npx wrangler deploy --env production",
    "ci:deploy": "npx wrangler deploy --env production",
    "ci:preview": "npx wrangler versions upload --env production",
    "broken-links": "mint broken-links"
  },
  "devDependencies": {
    "mint": "^4.2.616",
    "wrangler": "^4.16.0"
  },
  "overrides": {
    "react": "19.2.6"
  }
}
