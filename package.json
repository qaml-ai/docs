{
  "scripts": {
    "dev": "docsflare dev",
    "build": "docsflare build",
    "deploy": "docsflare deploy",
    "ci:deploy": "npx wrangler deploy --env production && npm run ci:search",
    "ci:preview": "npx wrangler versions upload --env production",
    "ci:search": "bash scripts/ci-search-sync.sh",
    "search:sync": "docsflare search sync",
    "doctor": "docsflare doctor"
  },
  "devDependencies": {
    "docsflare": "https://github.com/qaml-ai/docsflare/archive/5de12547cf6688a8bcf621fcd30a0854b06e997d.tar.gz",
    "wrangler": "^4.16.0"
  }
}
